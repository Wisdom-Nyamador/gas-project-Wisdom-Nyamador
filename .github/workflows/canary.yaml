name: Canary Deployment
on:
  workflow_dispatch:

jobs:
  canary:
    runs-on: ubuntu-latest
    steps:
      - name: Demonstrate canary traffic shift
        run: |
          echo "Placeholder to demonstrate canary traffic shift"
          echo "Update config to route more traffic to canary."